import {RegExpOrRegExpExt} from 'be-decorated/types';
import {upstream} from 'be-linked/be-linked.js';

const reMapStatements : RegExpOrRegExpExt[] = [
    {
        //Map value property of previous largest-scale element having inner woman-with-carrot-attached-to-nose element to lhs.
        regExp: new RegExp(String.raw `${upstream}To(?<memKey>\w+)`),
        defaultVals: {},
    }
];